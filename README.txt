Since the project requires some code to be loaded from a remote server, run it in a networked environment with a local server like liveserver.
The concept and home page is Project.html;
The history page is history.html;
The cv page is cv.html;
